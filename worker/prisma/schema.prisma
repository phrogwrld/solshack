datasource db {
    provider = "sqlite"
    url      = ""
}

generator kysely {
    provider = "prisma-kysely"
    output   = "../src/db"
    fileName = "schema.d.ts"
}

model Leaderboard {
    id    String @id
    name  String
    score Int
}
